// The project's roster of real people who get credited on case studies.
// Deliberately outside shared/ — this is project data, not a reusable
// primitive. Surnames: Diana and Gui are not recorded elsewhere in this
// repo, so first names only until confirmed.

export const JOY = { src: '/team/joy.svg', name: 'Joy Sengupta' };
export const DIANA = { src: '/team/diana.svg', name: 'Diana' };
export const GUI = { src: '/team/gui.svg', name: 'Gui' };
